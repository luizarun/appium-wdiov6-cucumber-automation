class AndroidInfo {
    static deviceName() {
        return '<real device id>'; // pass the udid or devicename
    }

    static platFormVersion() {
        return '<platform version>'; // pass the platform version
    }

    static appName() {
        return 'ZALORA_com.zalora.android.apk'; // pass the apk name
    }
}

module.exports = AndroidInfo;
